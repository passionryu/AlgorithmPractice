2 이해하기 쉬운 쉬운 말 개념 정리

비유: 미로에서 “한 길로 계속 들어가다가 막히면 한 걸음씩 뒤로 나와 다른 갈림길로 들어가는 방식”.

차이 vs BFS: BFS는 가까운 것부터(레벨 단위), DFS는 한 갈래를 끝까지 파고듬(스택 사용).

언제 쓰는가: 모든 해를 찾거나(모든 경로), 깊이가 중요한 탐색, 백트래킹 문제(퀸, 퍼즐), 연결성/사이클/위상정렬.

주의: DFS는 최단 경로를 보장하지 않음(그래프가 무가중치라 해도 BFS가 최단 경로 보장).